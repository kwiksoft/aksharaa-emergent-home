PF & ESI RETURNS FILING — SERVICE PAGE SECTION REFERENCE
URL: /compliance-filings-taxation/pf-esi-returns-filing
File: frontend/src/pages/PfEsiReturnsFiling.jsx
Role: TEMPLATE — section structure/patterns here propagate to the other
1 remaining service under Compliance, Filings & Taxation (smallest
category — only 2 services total) once locked.

Here's the full section list, top to bottom, exactly as they appear on the page:

| # | Section | testid | Theme | Current motion/treatment |
|---|---------|--------|-------|---------------------------|
| 1 | **Hero** — orange left accent bar, spacious headline, "Upcoming Filing Deadlines" clock card right with urgency-tinted top row | `returns-hero-section` | Light (most spacious template) | Staggered fade-up on load; deadline rows animate sequentially |
| 2 | **What We File** — 2 filing-stream cards (PF / ESI) each with dark header + detail rows | `returns-what-section` | Light (mist grey) | Scroll-reveal, staggered stream cards |
| 3 | **Filing Calendar** — unique month-grid (Every Month / April / May / November), active month highlighted orange | `returns-calendar-section` | Light | Scroll-reveal, staggered month tiles |
| 4 | **Scope** — numbered vertical-line process (4 steps) + dark "Filing Guarantee" card + "Contribution Rates" table | `returns-scope-section` | Light (mist grey) | Scroll-reveal, connected line + staggered steps |
| 5 | **Penalties** — 2-column PF/ESI penalty blocks + embedded mid-section CTA strip | `returns-penalties-section` | **Dark** (slate) | Scroll-reveal, staggered blocks |
| 6 | **FAQs** — two-column: client photo collage left, accordion right (open item gets calendar+clock icon + peach tint); bottom CTA strip with headset icon | `returns-faqs-section` | Light | Scroll-reveal on load; height-animate on expand/collapse |
| 7 | **Related Services** — 3-card grid | `returns-related-section` | Light (mist grey) | Scroll-reveal, staggered |
| 8 | **Final CTA** — compact dark band, heading + 2 CTAs | `returns-cta-section` | **Dark** (ink) | Scroll-reveal, single fade-up |

---

HOW TO GIVE FEEDBACK PER SECTION

1. Reference the section by number or testid (e.g. "Section 3" or
   "returns-calendar-section").
2. Describe the change in words, AND/OR share a screenshot of the current
   state with a markup/note on what's wrong, AND/OR share a reference URL
   + screenshot of the specific section you want studied.
3. Specify whether the change is:
   a. Visual/design only (colour, spacing, sizing, image)
   b. Content/copy
   c. Motion/animation (entrance, hover, scroll behaviour)
   d. Structural (add/remove/reorder elements within the section)

---

SECTION STATUS (update as we go)

| # | Section | Status |
|---|---------|--------|
| 1 | Hero | Correction round applied (this thread, on top of commit e9a5f06): removed the sub-paragraph under the headline, the 450+/0/15+ stats band, the description line under each deadline row, and the card footer strip — none of these appear in the client reference image. Deadline card header rebuilt as two-line "UPCOMING" / "FILING DEADLINES" with flanking accent lines and a line-dot-line divider, matching the reference exactly. Replaced the orbiting dashed-ellipse "swoosh" with a small dotted ring concentric with the peach circle. Badge seal resized up and repositioned higher/left over the chest, overlapping the peach circle's edge. Model and peach circle scaled up significantly (model wrapper 260x165→fine for mobile, desktop 420x260→560x340) so the visual carries the same weight as the reference; grid column ratio rebalanced from 1.7fr/1fr/1.3fr to 1.65fr/1.1fr/1.05fr to fit the larger centre visual — caught and fixed a headline word-wrap regression this introduced (em-dash was wrapping onto its own line) before finalising. All concentric-alignment claims (model/peach-circle/dotted-ring sharing a horizontal center, badge offset correctly) verified via Playwright bounding-box center coordinates, not visual inspection alone. Verified at 1440px/768px/390px. SECOND correction round (this thread): three direct instructions — (1) make every heading bolder/more stylish: main H1 and the deadline card's "UPCOMING"/"FILING DEADLINES" all tightened letter-spacing, given a subtle text-shadow for depth, and rebalanced in size (font-weight was already at Tailwind's font-extrabold ceiling, so the "bolder" read had to come from tracking/shadow/scale rather than weight alone); (2) model made noticeably bigger on both desktop (365x560→420x610 wrapper) and mobile (175x260→200x290), proportionally; (3) peach circle reduced (desktop 17rem→13.5rem, mobile 8.5rem→6.8rem) so it reads as an accent behind the model rather than a dominant shape. Increasing the heading font sizes on the first pass re-broke both the H1's 2-line wrap and the card's "FILING DEADLINES" line — caught immediately via screenshot, fixed by dialing both font sizes back down while keeping the tighter tracking/shadow treatment, landing on a size that's visually bolder than before without breaking either wrap. Concentric alignment of the resized model/peach-circle re-verified via Playwright bounding-box centers (both at x=801.1px) after the resize, not assumed. Verified at 1440px/768px/390px. Awaiting final user confirmation. |
| 2 | What We File | Full rebuild per client reference image (this thread). Changed from stacked vertical cards to side-by-side themed PF (navy)/ESI (red) cards with dark header bands, tinted icon-chip rows, and tinted "Monthly + Annual" pills. Header emblems in the reference are the actual EPFO/ESIC government logos — substituted with generic cog/heart icons (same circular badge treatment) rather than reproducing real government insignia; flagged in the component's docblock. Copy corrected to match reference exactly (kicker "Three Streams. One Commitment.", two-tone heading, sub-copy) — old data's sub said "Three statutory filing streams... PF, ESI, and Professional Tax" which didn't match the page (only 2 streams exist) or the reference (which says "Two statutory filing streams — PF and ESI"). Added a 4th ESI row ("Challan Generation & Payment") that the reference shows but the old data was missing, so both cards are 4 rows now. 8 new colour tokens added to tailwind.config.js (returnsPf*/returnsEsi* — header band, icon chip bg, icon stroke, pill text per theme), all sampled via PIL from the reference image with documented provenance, not eyeballed. Bug caught and fixed mid-build: the shared `calendar` icon (Lucide CalendarClock) read like a tiny camera at row-icon size — added a new `calendarPlain` icon key (plain Calendar glyph) rather than remapping `calendar`, since 3 other components already depend on the existing CalendarClock mapping. Also fixed an initial heading word-wrap regression (container too narrow for the 1-line reference heading at the original font size). Verified at 1440px/768px/390px. Awaiting final user confirmation. |
| 3 | Filing Calendar | Full rebuild per client reference image (this thread). Added an entirely new two-column intro (kicker/heading/sub left, custom inline-SVG desk-calendar illustration right) that the old version didn't have at all — old version was month-tiles only. Month tiles rebuilt: circular icon badges (orange for the active "Every Month" tile, navy for the rest) replacing the old flat-colour-block style, a line-dot-line divider under each month label, a right-pointing arrow connector between the active tile and April (desktop only — hidden once tiles wrap to 2 columns, since the "next in sequence" visual doesn't hold once the grid wraps), and a faint ghost calendar-icon watermark in each tile's bottom-right corner. Desk-calendar illustration built as a custom SVG (card body, orange 3D side-edge, chunky metal binder rings, icon badge, headline text, note pill) rather than a raster asset. Went through one real revision: first attempt rendered the binder rings as thin paperclip-like loops and used the same CalendarClock icon that caused the camera-look bug in Section 2 — both fixed (rings rebuilt as chunkier ellipse+rect loops, icon swapped to the new `calendarPlain` key introduced for Section 2). One new colour token added (ak.returnsCalendarBadge #F57418, PIL-sampled from the "Every Month" badge fill, 2218-point average) — the inactive-tile navy badge reuses the existing ak.ink token rather than adding a near-duplicate (reference sample #1D293D vs ak.ink #1C2A39, within anti-aliasing noise). A "Filing Rhythm" kicker visibility issue spotted during tablet review turned out to be a screenshot-tool scroll artifact (Playwright's scrollIntoViewIfNeeded landed the section flush under the sticky header) rather than a real page bug — confirmed by re-screenshotting with proper sticky-header scroll compensation; no code change needed for that one. Verified at 1440px/768px/390px. Awaiting final user confirmation. |
| 4 | Scope | Full rebuild per client reference image (this thread). Changed from a simple 2-column layout (steps+heading left, Guarantee card+contribution table stacked right) to a 2-row structure matching the reference exactly: Row 1 is 3 columns — a photo stack (left), kicker/heading/sub plus a new "Trusted Experts For Your Compliance" card with a "Learn More" CTA and a "100%" satisfaction badge (centre), and the dark "Filing Guarantee" card spanning the full row height (right). Row 2 is 2 columns — the numbered steps and the contribution table, each now in its own bordered white card. All existing copy (kicker, heading, sub, all 4 step titles/descriptions, all 6 guarantee items, contribution table, footnote) already matched the reference exactly — no copy changes needed, only the new "Trusted Experts" card content was added to the data file. Photo placeholders: the reference's two photos (a team member in an Aksharaa-branded polo giving a thumbs up, and a close-up of labelled "PF RETURNS"/"ESI RETURNS" binders) are specific brand photography not available as source files — built as clearly-labelled icon placeholders (thumbs-up icon / folders icon) in the page's palette rather than generating a fake photorealistic "branded employee" image; flagged in the component's docblock with the exact asset paths to drop the real photos into when available. 3 new colour tokens added (returnsScopeNavy, returnsScopeBlueCard, returnsScopeBlue), PIL-sampled from the reference with documented provenance. 2 bugs caught and fixed mid-build: (1) the photo-stack's two placeholder cards showed a visible diagonal "seam" where they overlapped — root-caused via a flat-colour test (proved it was gradient banding from from-ak-ink/to-ak-navy and from-ak-orange/to-ak-orange-dark being too large a colour jump at that angle, not a z-index/stacking bug) and fixed by using closer-toned top-to-bottom gradients instead; (2) the Filing Guarantee card briefly appeared washed-out grey in an early screenshot — confirmed via computed-style inspection that the actual background colour was correct (rgb(2,33,63), opacity ~1) and the grey appearance was a Reveal/scroll-animation capture-timing artifact, not a real rendering bug; re-confirmed clean with a longer settle delay before screenshotting. Verified at 1440px/768px/390px. Awaiting final user confirmation. |
| 5 | Penalties | Full rebuild per client reference image (this thread). Previous version was a single flat dark block with no PF/ESI theming distinction (both columns looked identical) and no header illustration. Rebuilt to match the reference: a custom inline SVG/HTML illustration (alarm clock face reading "Don't Delay", two floating "Interest"/"Penalty" badge cards, a stack of "PF Return"/"ESI Return" document cards), two distinctly themed penalty cards (PF navy, ESI red) each with its own icon-chip rows, and a bottom "Already behind on filings?" CTA strip with a shield-check icon — replacing the previous plain text+button row. Heading split into two lines ("Cost of Late or" / "Missing Filings", second line in orange) to match the reference exactly. All existing copy (kicker, sub, both block labels, all 6 items with their exact rates/section numbers, CTA text/label) already matched the reference exactly — no copy changes needed beyond the heading split and adding icon keys per row for the new chip treatment. Colours: reused returnsScopeNavy/returnsEsiHeader/returnsPfChip/returnsPfIcon/returnsEsiChip/returnsEsiIcon from Sections 2 and 4 rather than adding near-duplicates (all converged within normal sampling variance when checked against this reference); only one genuinely new token added (returnsPenaltyCard, the cards' neutral light-grey background). Two real bugs caught and fixed via Playwright bounding-box measurement, not just visual guessing: (1) the clock's bell shapes and "feet" rendered in the wrong place because the wrapping motion.div had no explicit size, so absolutely-positioned children measured from the wrong box — fixed by giving that wrapper an explicit h-40 w-40; (2) one bell visually collided with the document-card stack (confirmed bell right-edge at x=1161px vs doc-card left-edge at x=1149px, an actual overlap) — fixed by repositioning the document cards further right/down, re-verified clear by coordinates (1166px vs 1161px) after the fix. Verified at 1440px/768px/390px. CORRECTION ROUND (this thread, commit 56c9201): client sent the original source illustration image + a full reference mock + the standalone button graphic, with 3 specific corrections. (1) Badge outline: Interest/Penalty boxes' border bumped white/10→white/25 to match the clearly-visible thin outline in the reference (previous value was too faint to read). (2) Orange colour: PIL-sampled the reference at heading text/clock text/shield icon, converged tightly on rgb(251,102,12)/#FB660C — checked against existing tokens per project colour-reuse discipline, landed within ~5 units/channel of the existing ak.orange2 (#FF6B05) vs a 9/38/28 diff against ak.orange (#F28C28), so reused ak.orange2 rather than adding a new token; applied to every orange accent in the section (heading, underline, clock text, badge icons, glow, checkmark, bottom shield), not just the one element named. (3) Arrow button: client confirmed this should be a GLOBAL change to the shared AkButton component (24 usages site-wide), not scoped to this section alone — added a white-circle chip behind the arrow with per-variant arrow colour (orange for primary). Verified at 1440px/390px; mobile note — the illustration is hidden below `lg` so badge-outline visibility was confirmed via Playwright bounding-box crop at 1440px instead. Committed and pushed. SECOND CORRECTION ROUND (this thread, commit 66548d5): client clarified two earlier asks that hadn't actually been done — "clock insertion" meant replacing the hand-built SVG clock with the literal clock_and_files.png image sent earlier, and "cards twist" meant rotating/fanning the PF/ESI document cards. Both resolved by the same change: swapped in the real image (confirmed true alpha transparency; downscaled 1536x1024→900x600 and optimised 2.3MB→~250KB to match the page's other asset sizes; saved to frontend/public/assets/sections/returns-penalties-clock.png), which already shows the document cards naturally fanned/tilted — no separate rotation code needed. Removed the now-unused DocCard component and hand-built clock markup; left clockLine1/clockLine2/documents data fields untouched (confirmed no other consumers via grep) since removing them wasn't necessary for a presentation-only change. Real bug caught and fixed via Playwright measurement: first attempt kept the old fixed-height absolute-overlay layout and just dropped the new image in, causing a genuine ~52px overlap between the image's bottom edge (y=548.8px) and the PF/ESI card row's top edge (y=496.3px) — same class of collision as the original Section 5 build's bell/doc-card bug. Root cause was the short fixed-height container not accounting for the real image's 1.5:1 aspect ratio at a legible size. Fixed by restructuring from absolute-positioned overlay to a flex row (badges left, image right, natural height) rather than re-guessing a taller fixed box; re-measured clean after the fix (67px gap). Also sized the image up from an initial too-small 220x330 attempt to a 300px-wide render, matching the reference's proportions where the illustration reads as the column's visual anchor rather than a small corner accent. Verified at 1440px/768px/390px — illustration correctly hidden below `lg` on tablet/mobile, no regression. Committed and pushed. COPY CORRECTION (this thread, commit e326dba): prompted by a general "correction needed in some section" request with no section specified — re-checked Penalties and FAQs copy systematically word-for-word against their reference images rather than guessing which section needed work. Found two real mismatches predating this thread, present since the original Penalties rebuild: (1) CTA button label was "Talk to Us Now" (lowercase to/capital Us), reference shows "Talk To Us Now" — corrected, also now matches the FAQs section's identical button casing; (2) sub-headline had "Every day of delay **on** PF and ESI filings", reference reads "...**in** PF and ESI filings" — corrected. No other copy discrepancies found after checking every line in both sections against their references. Committed and pushed. |
| 6 | FAQs | Full rebuild per client reference image (this thread, commit 28f2103) — first reference image provided for this section. Previous status was baseline content only (from original template build, commit 6c4c115) with no reference-image rebuild. Changed from a centered single-column accordion with no imagery to a two-column layout: client-supplied photo collage (left, Faq_image01.png — a single flattened RGB composite used as-is, downscaled 1403x1121→1000x799 and re-saved as optimised JPG, placed at frontend/public/assets/sections/returns-faqs-collage.jpg) + accordion (right). Kicker corrected "Common Questions"→"FAQ's" with a new chat-bubble icon (messagesSquare) — switched wrapping class ak-kicker→ak-kicker-bare since ak-kicker has its own dash-line ::before pseudo-element that would have clashed with the new icon, caught before building. Open accordion item: peach-tinted background, calendar+clock icon (reused existing `calendar`/CalendarClock mapping, checked registry first per discipline), minus-circle toggle. Closed items: plain white card, plus-circle toggle. Heading/sub/all 5 Q&As already matched the reference exactly — no copy changes needed. New bottom CTA strip ("One Point of Contact. Timely Filings. Complete Compliance." + button with solid-orange headset-icon circle + dotted-pattern fade), same structural pattern as ReturnsPenalties.jsx's CTA strip but filled icon circle instead of outlined ring, per this reference. One new colour token: returnsFaqTint (#FEF8F5), PIL-sampled from both the open-item background and the CTA strip background, which converged on the same value — reused in both places. New icons registered: messagesSquare, minus, plus (replacing previous reuse of "x"/"arrowUpRight" for toggle purposes). Real bug caught and fixed via screenshot review: first attempt additionally rendered a hand-built "15+ Years of Experience" badge + a separate hand-built dotted-circle decoration on top of the collage image — re-inspecting the supplied asset directly confirmed both are already baked into the composite, causing a visible duplicate-badge bug once layered together; fixed by removing the redundant overlay entirely. Left the now-unused `experienceBadge` data field untouched (confirmed no other consumers via grep) rather than deleting it for a presentation-only fix. Verified at 1440px/768px/390px — collage correctly hidden below `lg` on tablet/mobile (degrades to single-column accordion, no broken elements). Confirmed accordion open/close interaction still works correctly post-rewrite (tested via actual click, not assumed) and that conditional icon rendering behaves correctly for items without an `icon` field. Confirmed zero console/page errors on load. Committed and pushed. CORRECTION (this thread, commit 0998a18): client asked to increase the collage image's height to match the accordion column's height. Measured the actual mismatch before changing anything — image was 358px tall (h-auto, natural 1000x799 ratio) vs a 717.5px right column. Fixed by switching the parent grid from items-center to items-stretch and the image to h-full/object-cover, so it fills the full column height by cropping the sides rather than distorting the aspect ratio. Re-measured after: both columns at 717.5px, tops aligned within 0.2px. Real tradeoff surfaced to client before committing: this crop removes roughly half the image's width, visibly trimming "PF"/"ESI" off the binder labels (now reads "RETURNS" only) and cropping part of the calculator photo — client confirmed this is acceptable, height match is the priority. Verified at 1440px/768px/390px — tablet/mobile unaffected since the collage is hidden below `lg` at both. Committed and pushed. |
| 7 | Related Services | Baseline content exists (same commit 6c4c115) — 3 real related-service cards with correct internal links and on-brand copy. NOT yet rebuilt against a specific client reference image — none has been provided for this section. Corrected from prior thread's ambiguous "Pending review" phrasing. |
| 8 | Final CTA | Baseline content exists (same commit 6c4c115) — real heading/sub/CTA copy and working buttons. NOT yet rebuilt against a specific client reference image — none has been provided for this section. Corrected from prior thread's ambiguous "Pending review" phrasing. |
