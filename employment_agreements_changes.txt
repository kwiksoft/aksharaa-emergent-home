EMPLOYMENT AGREEMENTS — SERVICE PAGE SECTION REFERENCE
URL: /legal-documentation/employment-agreements
File: frontend/src/pages/EmploymentAgreements.jsx
Role: TEMPLATE — section structure/patterns here propagate to the other
17 services under Legal & Documentation once locked.

Here's the full section list, top to bottom, exactly as they appear on the page:

| # | Section | testid | Theme | Current motion/treatment |
|---|---------|--------|-------|---------------------------|
| 1 | **Hero** — warm parchment background, headline + trust-item checklist, Mac-window styled document-preview card with skeleton lines + highlighted clause | `agreements-hero-section` | Light (warm parchment #FAF6EE) | Staggered fade-up on load; document card rotates in; section/clause lines animate sequentially |
| 2 | **Agreement Types** — 6-card bento (Permanent / Fixed-Term / Consultancy / Apprentice / CXO / Offer Bundle) with tag pills | `agreements-types-section` | Light | Scroll-reveal, staggered grid |
| 3 | **Key Clauses** — 9-item legal table-of-contents (§1-§9) with Essential/Critical tier badges | `agreements-clauses-section` | **Dark** (ink) | Scroll-reveal, staggered list |
| 4 | **Scope** — 6-item "What Aksharaa Delivers" grid | `agreements-scope-section` | Light (mist grey) | Scroll-reveal, staggered grid |
| 5 | **Process** — 4-step numbered list + "Turnaround Times" card + "Delivered In" formats card | `agreements-process-section` | Light | Scroll-reveal, staggered steps + cards |
| 6 | **FAQs** — accordion, single-open pattern | `agreements-faqs-section` | Light (mist grey) | Scroll-reveal on load; height-animate on expand/collapse |
| 7 | **Related Services** — 3-card grid | `agreements-related-section` | Light | Scroll-reveal, staggered |
| 8 | **Final CTA** — compact dark band, heading + 2 CTAs | `agreements-cta-section` | **Dark** (ink) | Scroll-reveal, single fade-up |

---

HOW TO GIVE FEEDBACK PER SECTION

1. Reference the section by number or testid (e.g. "Section 3" or
   "agreements-clauses-section").
2. Describe the change in words, AND/OR share a screenshot of the current
   state with a markup/note on what's wrong, AND/OR share a reference URL
   + screenshot of the specific section you want studied.
3. Specify whether the change is:
   a. Visual/design only (colour, spacing, sizing, image)
   b. Content/copy
   c. Motion/animation (entrance, hover, scroll behaviour)
   d. Structural (add/remove/reorder elements within the section)

---

SECTION STATUS (update as we go)

| # | Section | Status |
|---|---------|--------|
| 1 | Hero | FULL REBUILD (this thread), per client reference image + supplied background asset. Replaced the previous document-preview-card concept entirely: removed the Mac-window styled skeleton-document card, replaced with a full-width background photo (hand signing a document on a clipboard) spanning the section's upper area. Background asset (agreements-hero-bg.jpg, sourced 1672x941, saved at 1920x1081) confirmed via direct crop-diffing to contain ONLY the photo + soft vignette + faint city-skyline watermark + left-side orange hexagon decorations — no shield, rings, dots, or badges baked in, so all of those were built as foreground elements rather than risking duplication. Initial build wrongly treated the asset as a boxed right-side panel with hard rounded corners; corrected after re-examining the reference (which shows the photo fading to nothing via vignette, no hard edge) and the asset's own wide/landscape ratio (only sensible as a full-section background, not a narrow side panel) — rebuilt with the image as the section's own absolutely-positioned background, masked with a bottom gradient fade into the parchment rather than a hard cutoff. Left column: headline kept page-specific ("Employment Agreements", not the reference's generic "Legal Agreements") per direct confirmation; added an orange line-dot divider matching the pattern used on other template heroes; sub-copy shortened to the reference's 2-line version; old single-line 4-item trust checklist replaced with a 3-item RICH format (icon box + bold title + description, divided by thin separators) — exact 3 items from reference (Legally Vetted / Role-Specific & Precise / Court-Enforceable); second CTA changed from a neutral-grey outline to an orange-bordered outline (inline override, not a new global AkButton variant, since this look is specific to this Hero); both CTA buttons reduced in padding/font-size after they wrapped to two lines in the narrower post-rebuild column (302+286px combined exceeded the 370px column — confirmed via Playwright bounding-box measurement, not guessed). Right side: grid ratio corrected from 1.05fr/0.95fr to 0.92fr/1fr to better match the reference's wider visual column. New foreground elements added: a semi-transparent orange shield+checkmark centrepiece (custom inline SVG, not a raster asset), two independently-rotating dotted decorative rings with dot markers (same treatment as the PF & ESI Returns Filing hero's dotted ring), a small isolated orange dot, a bottom-right dot-grid watermark, and 4 floating white badge cards (Confidentiality Protected / Your IP Your Rights / Legally Vetted by Experts / Clear Terms Fewer Disputes) at fixed positions. Shield position required two correction rounds: first attempt placed it directly over the hand/pen (wrong — reference shows it in clear document space beside the hand); second attempt also overlapped after a background-ratio fix shifted the visible photo crop; final position derived from direct visual inspection of this build's own rendered photo crop (which necessarily differs from the reference's exact crop, since the container's aspect ratio differs) rather than from the reference's raw pixel coordinates — confirmed clear of the hand via a fresh screenshot. New bottom trust strip added (4 items: Delivered in 3-5 Days / One Revision Included / Expert Legal Support / Built for Your Business, thin vertical dividers, 2x2 on mobile) — did not exist in any form before. New colour tokens added (ak.agreementsOrange #FA8021, ak.agreementsStripBg #F6F0EB), both PIL-sampled from the reference with documented provenance in tailwind.config.js. New icon added (lightbulb, for "Your IP, Your Rights"); clipboardCheck reused for "One Revision Included" rather than adding a near-duplicate file-check icon. Verified at 1440px (desktop, photo/badges visible), 768px (tablet, photo/badges hidden, clean single-column stack), and 390px (mobile, same). Zero console errors at all three. One repeated visual artifact (Playwright's scrollIntoViewIfNeeded landing the kicker behind the sticky header in section-cropped screenshots) confirmed as a known tooling quirk, not a real bug, via a full-viewport screenshot at natural scroll — consistent with the same artifact already documented on the PF & ESI Returns Filing page. CORRECTION ROUNDS (this thread, post-build): (1) shrunk shield+checkmark and shifted right, moved all 4 badges left to cluster around the orbit area instead of floating near the photo's right edge — required a follow-up nudge after the first pass caused 2 badges to overlap the hand/each other; (2) shrunk shield further and recentred it precisely on the dotted ring's actual centre point, recalculated all 4 badge positions to sit exactly on the ring's circumference (one badge, "Clear Terms, Fewer Disputes", had drifted off the ring entirely in the previous round); (3) raised "Your IP, Your Rights" and raised+shifted "Clear Terms, Fewer Disputes" left per final positioning feedback — first attempt at the new Clear Terms position landed on the hand, corrected by pulling further left. **CLIENT SIGNED OFF — marked complete.** |
| 2 | Agreement Types | Background image added (this thread), per client request. Client-supplied handshake-over-contract photo (agreements-types-bg.jpg, source 1366x768, saved at 1920x1079) applied as the section's full background, with an absolute white/85% tint overlay on top so the kicker/heading/sub text and card content stay fully readable against the photo's visible contract text and concentric ring decorations. Cards keep their existing opaque parchment fill (#FAF6EE) rather than getting a new treatment, since their own solid background already reads cleanly above the tinted photo. Verified at 1440px (photo visible at low opacity in the gaps around cards and at the section edges, text fully legible), 768px and 390px (photo mostly washed out at these crops, no readability issues, no layout regressions). Zero console errors at all three viewports. No other changes to this section's structure, content, or card layout. CORRECTION (this thread): tint opacity reduced from white/85% to white/60% per direct feedback to make the photo more visible — handshake, contract text, and ring decorations now clearly visible while all text/cards remain fully readable. Verified at 1440px and 390px, zero console errors. **CLIENT SIGNED OFF — marked complete.** |
| 3 | Key Clauses | FULL REDESIGN (this thread), per client reference image, with explicit instruction to keep the new illustration flat/simple rather than replicating the reference's 3D render. Switched from dark theme (bg-ak-ink) to light theme (#FFFCF9) matching the page's warm parchment palette. Heading split for an orange-emphasis word ("Must Have") with a hand-drawn-style squiggle underline (inline SVG path, not a straight rule) beneath it. New illustration block added (top-right, didn't exist before): flat-style clipboard card with 4 checklist rows + a pen icon, an orange checkmark badge, and 3 small orbiting icon circles (person/scale/document) connected by dashed arc paths — built as simple SVG/flexbox shapes per direct instruction, not a raster 3D mockup. Each of the 9 clause rows redesigned: added a colour-circle icon per clause (new `icon` field in the data file — user/calendar/wallet/clock/shield/lightbulb/ban/bell/gavel, with `ban` substituted for the reference's no-entry glyph on Non-Solicitation), switched numeral display from "§ N" to plain "01"-"09" (added a new `display` field; `num` retained as unused content metadata), added a vertical divider between icon and numeral. Tier pills redesigned: Essential is now an outlined pill (border + text in the page's existing accent orange, transparent fill, replacing the old translucent-fill treatment); Critical is now a solid filled pill in a new genuinely-distinct deeper rust-orange (ak.agreementsCritical #DB763B, PIL-sampled from the reference with documented provenance — confirmed distinct from the page's existing agreementsOrange #FA8021 used for icons/numerals, not a near-duplicate). Footnote moved out of the bordered list into its own separate boxed card below it, with a light peach background (ak.agreementsIconBg #F7E4D5, reused for both the icon-circle and footnote-card backgrounds per the reference, which uses the same tint in both places). New icons added: bell, ban (lightbulb/gavel/user/calendarPlain/wallet/clock/shield/fileText/scale/checkCircle/edit3 already existed). Kept existing clause copy unchanged throughout (client confirmed not to match the reference's slightly different wording). Caught and ruled out a false-positive during review: the "ESSENTIAL" pill text appeared to have an odd gap ("ES SENTIAL") in a small mobile screenshot crop — verified via exact Playwright bounding-box coordinates that this is the tracking-wide letter-spacing rendering normally at small size, not a real text-wrap bug; added whitespace-nowrap as harmless extra insurance regardless. Verified at 1440px (illustration + full row layout), 768px and 390px (illustration correctly hidden, clean single/stacked-column row layout) — zero console errors at all three. Confirmed no overlap with neighbouring sections (Agreement Types above, Scope below). **CLIENT SIGNED OFF — marked complete.** |
| 4 | Scope | FULL REDESIGN (this thread), per client reference image. Previous version: plain uniform 3-col grid, every card visually identical (white bg, small orange icon box, no numerals, no header decoration, no bottom strip). Switched to a 2-col layout (6 cards, 2 per row across 3 rows) with each card carrying a large numeral (01-06, new `display` field) and one of 4 alternating fill treatments (new `fill` field: gold solid / rust solid / dark solid / plain bordered-light) — reproducing the reference's specific, non-obvious alternating pattern exactly (01 gold, 02 light, 03 light, 04 rust, 05 dark, 06 light) via direct pixel sampling rather than a generic nth-child alternation. Two new colour tokens added (ak.agreementsGold #D29D2D, ak.agreementsRust #D85604), both PIL-sampled from the reference with documented provenance in tailwind.config.js — confirmed genuinely distinct from the existing agreementsOrange/agreementsCritical tokens via direct comparison, not assumed reuse. The dark card (05) reuses the page's existing ak.ink rather than a new token (sampled fill within normal tolerance). Heading split for an orange-emphasis word ("Aksharaa"), matching the [part1, part2, part3] pattern used on other rebuilt sections. Icons re-mapped to match the reference's actual glyphs: item 2 switched fileSignature→clipboardCheck (reference shows file+checklist+pen, distinct from item 1's plain file+pen); item 6 switched checkCircle→new `handshake` icon (reference shows a handshake with no existing equivalent in the icon registry; added as a new lucide-react import rather than reusing an unrelated glyph). Item 5 (HR Policy) kept on fileText — reference shows a file+bookmark glyph with no close existing match, not deemed worth a new near-duplicate icon import for a single card. New header decoration: a dot-grid watermark (top-right, desktop only, `lg:block`) built as a simple inline SVG grid of dots — no existing dot-grid precedent in the codebase, built fresh per the project's established "flat SVG shapes, no raster" convention (same approach as Section 1's dotted rings and Section 3's orbit illustration). New bottom trust-strip block added (didn't exist in any form before): single bordered card spanning full width, 4 items (Legally Precise / Role-Specific & Flexible / Quick Turnaround / Ongoing Legal Support — reference's text used as-is per direct instruction to continue), each with a circular icon badge alternating gold/rust fill, divided by thin vertical dividers on desktop (`md:divide-x`) collapsing to a 2x2 grid with no dividers on mobile/tablet. New `scopeTrustStrip` data export added; reuses 4 existing icons (shield, userCheck, clock, headset) — no new icons needed for the strip itself. Verified at 1440px (desktop, 2-col grid + dot-grid watermark + full-width strip with dividers), 768px (tablet, 2-col grid retained, strip falls back to 2-col stack, no orphaned divider artifacts), and 390px (mobile, single-column stack, strip 2x2). Zero console errors at all three viewports. One repeated visual artifact (Playwright's scrollIntoViewIfNeeded landing Card 01 partly behind the sticky header in section-cropped mobile screenshots) confirmed as the same known tooling quirk already documented on the Hero (Section 1) and Key Clauses (Section 3) sections, via a full-viewport natural-scroll screenshot showing clean rendering with no real overlap. **Pending client sign-off.** |
| 5 | Process | FULL REDESIGN (this thread), per client reference image (WhatsApp-compressed JPEG — sampled with that lower fidelity in mind, vs the clean PNGs used for Sections 3-4). Previous version: plain numbered-circle (uniform single colour) + text list on the left, no photo banner, flat Turnaround row list with inline trailing time text, Delivered In card using a repeated generic fileText icon for both formats. New top photo banner added (didn't exist before): reuses the Hero's own background asset (agreements-hero-bg.jpg) at full strength instead of a new client-supplied asset, since direct visual comparison confirmed it's the same underlying photograph the reference shows for this section, just without Hero's fade mask — 4 floating icon badges (people/document/scales/shield-check) over the photo connected by a faint dashed orbit arc, reusing the Hero's exact badge-layer construction (absolute positioning by percentage, white rounded-card chrome) rather than a new treatment. Left column: 4-step timeline rebuilt — each step's numbered circle is now one of 4 distinct new colours (orange/blue/green/purple, new `accent`+`tint` hex pairs per step, PIL-sampled from the reference's badges) instead of the old uniform colour, connected by a single continuous dashed line spanning the full column (CORRECTION, this thread: first attempt used a per-row flex-1 spacer that rendered with zero height since flex-1 had no defined parent height to stretch into — invisible in the rendered screenshot; fixed by switching to one absolutely-positioned line spanning the whole RevealGroup, sitting behind the circles via z-index). Each step also gets a small coloured icon-box above the title (new), with icons mapped to the reference's actual glyphs (clipboardList / fileClock [new icon import] / messagesSquare / send). Per direct instruction, used icon-box placeholders in the position where the reference shows a real photo per step — no image-generation tool available in this environment; swapping in real photos later only needs an `image` field per step and an &lt;img&gt; swapped in for the icon box, no other structural change. Right column: Turnaround Times card redesigned with a dotted timeline connector + small orange dot bullets per row (CORRECTION, this thread: same flex-1-with-no-height bug as the step timeline, caught during review and fixed the same way — one continuous absolutely-positioned dotted line behind all 5 bullets) and a two-line duration pill (bold range + smaller "working days" label, new `range`/`unit` data fields replacing the old single `time` string) replacing the old inline trailing text; footnote moved into its own bordered/tinted box with an info icon (was a plain trailing paragraph before). Delivered In card reuses the page's existing ak.ink (sampled reference value, within normal tolerance once accounting for JPEG compression on a phone photo) rather than a new token, gets a new dot-grid watermark (top-right, same construction as Section 4's), an outlined download icon + orange underline under the heading, and the two format rows (new `ext`/`label` structure replacing plain strings) now show a flat colour-coded file-type badge (blue "WORD" / red "PDF") built as plain coloured boxes with text — not a reproduction of Microsoft's or Adobe's actual logo artwork. New icon added: fileClock (clipboardList, messagesSquare, send, scale, shield, users, fileText, clock, download, alertCircle all already existed). Verified at 1440px (banner + orbit badges visible, both connector lines continuous, full 2-col layout), 768px (banner photo column correctly hidden per lg:block, single-column step stack, both card types stack cleanly below), and 390px (single column throughout, connectors intact, no overlap). Zero console errors at all three viewports. Same known scrollIntoViewIfNeeded/sticky-header artifact already documented on Sections 1, 3, and 4 reconfirmed here via natural-scroll screenshot — not a real bug. **Pending client sign-off.** |
| 6 | FAQs | FULL REDESIGN (this thread), per client reference image. Previous version: single full-width white accordion card with no left illustration column, x/arrowUpRight toggle icons sharing the same border-only treatment for both open and closed states (just colour-inverted), plain bg-ak-mist/40 section background. Switched to a 2-col layout: left column keeps the kicker/heading/sub and gains a new illustration (didn't exist before) — two overlapping flat speech-bubble shapes (large orange-gradient + small cream, each with a "?") with 3 small white icon-badges (scale/fileText/users, new `illustrationIcons` data field) orbiting on a dashed arc, reusing the exact orbit-badge construction pattern already established on the Hero (Section 1) and Process (Section 5) — absolute positioning by percentage, dashed SVG arcs — rather than a new technique; built as flat SVG shapes (gradient-filled paths + SVG text for the "?"), not a raster mockup, consistent with the project's no-raster convention. Right column accordion redesigned with a genuine open/closed visual distinction that didn't exist before: closed items are a plain white card with an orange-outlined (unfilled) plus-circle; the open item switches to a peach card background (reusing the page's existing #FAF6EE parchment tone — PIL-sampled reference value landed almost exactly on this already-used token, confirmed via direct colour-distance comparison against candidate tokens before deciding not to add a new one) with a solid agreementsOrange-filled minus-circle. Toggle icons switched from x/arrowUpRight to the already-registered minus/plus icon keys — no new icon imports needed. New section-level background decoration added (didn't exist before): a soft diagonal wave shape (flat SVG path, 5% opacity orange fill) and dot-grid watermarks in both bottom corners (hidden below sm: to avoid mobile clutter), section background switched from bg-ak-mist/40 to a near-white #FFFBF8 base matching the reference's much lighter tone. No content changes — all 5 question/answer pairs and the kicker/heading/sub matched the reference word-for-word already. Verified at 1440px (2-col layout, illustration + orbit badges visible, wave/dot-grid decoration visible), 768px (illustration correctly hidden per lg:block, single-column accordion stack, decoration still visible), and 390px (single column throughout, dot-grid correctly hidden per sm:block to reduce clutter, wave decoration intact, no overlap). Zero console errors at all three viewports. Accordion interaction tested directly (clicked a different FAQ item): correctly closed the previously-open item back to its white/plus state and opened the clicked item to its peach/minus state — confirmed working, not just visually styled. **Pending client sign-off.** |
| 7 | Related Services | Pending review |
| 8 | Final CTA | Pending review |
