PF & ESI REGISTRATION — SERVICE PAGE SECTION REFERENCE
URL: /registrations-licensing/pf-esi-registration
File: frontend/src/pages/PfEsiRegistration.jsx
Role: TEMPLATE — section structure/patterns here propagate to the other
25 services under Registrations & Licensing once locked.

Here's the full section list, top to bottom, exactly as they appear on the page:

| # | Section | testid | Theme | Current motion/treatment |
|---|---------|--------|-------|---------------------------|
| 1 | **Hero** — headline overlapping bleeding photo, animated "Registration Scope" tracker card (done/active/pending checklist) floating right, stat row beneath photo | `reg-svc-hero-section` | Light (cream) | Staggered fade-up on load; tracker steps animate in sequence with delay |
| 2 | **Overview** — "What is PF & ESI Registration", asymmetric 60/40 split, dark PF tile + light ESI tile, offset side card with alert callout | `reg-svc-overview-section` | Light | Scroll-reveal fade-up, side column delayed |
| 3 | **Applicability** — PF vs ESI side-by-side comparison cards, connected at center border (not tabs) | `reg-svc-applicability-section` | Light (mist grey) | Scroll-reveal, staggered left-then-right |
| 4 | **Documents** — 3-column checklist cards (Establishment / Employer / Employee docs), staggered vertical offset | `reg-svc-documents-section` | Light | Scroll-reveal, staggered columns (offset fix applied) |
| 5 | **Process** — 6-step vertical connected timeline with icon nodes | `reg-svc-process-section` | **Dark** (navy/ink) | Scroll-reveal, sequential top-to-bottom stagger |
| 6 | **Penalties** — PF/ESI penalty cards + integrated photo, asymmetric narrow/wide split | `reg-svc-penalties-section` | Light (mist grey) | Scroll-reveal, photo + cards staggered separately |
| 7 | **FAQs** — accordion, single-open pattern | `reg-svc-faqs-section` | Light | Scroll-reveal on load; height-animate on expand/collapse |
| 8 | **Related Services** — 3-card grid linking to adjacent services | `reg-svc-related-section` | Light (mist grey) | Scroll-reveal, staggered |
| 9 | **Final CTA** — compact dark band, heading + 2 CTAs | `reg-svc-cta-section` | **Dark** (navy) | Scroll-reveal, single fade-up |

---

HOW TO GIVE FEEDBACK PER SECTION

1. Reference the section by number or testid (e.g. "Section 4" or
   "reg-svc-documents-section").
2. Describe the change in words, AND/OR share a screenshot of the current
   state with a markup/note on what's wrong, AND/OR share 3-4 still frames
   from a reference video (start / mid-motion / end) plus one word for the
   motion feel (e.g. "snappy", "slow and floaty", "bouncy") if the change
   is about animation/timing rather than layout.
3. Specify whether the change is:
   a. Visual/design only (colour, spacing, sizing, image)
   b. Content/copy
   c. Motion/animation (entrance, hover, scroll behaviour)
   d. Structural (add/remove/reorder elements within the section)

Once a section is locked, mark it DONE below so we don't revisit unless
something new comes up.

---

SECTION STATUS (update as we go)

HERO — full history of changes made this thread (see below for details);
row 1 in the status table further down reflects the current state.

PHOTOS RECEIVED (this thread, follow-up): client supplied 3 real photos (plus a 4-image contact sheet to select from) matching the requested specs almost exactly — Photo 1 (1024x1024, man in glasses signing a document at a desk), Photo 2 (1024x1024, EPFO/ESIC registration documents on a desk with laptop/plant), Photo 3 (1024x687, two people in a handover meeting). Converted from the supplied PNGs (~950KB-1MB each) to optimised JPGs (quality 85, ~96-134KB each) to match this project's existing asset-size convention, saved to frontend/public/assets/sections/ as reg-hero-photo1.jpg, reg-hero-photo2.jpg, reg-hero-photo3.jpg. Data file and component comments updated to remove "temporary placeholder" language now that real photos are wired in. Re-verified at 1440px, 768px, and 390px with the real photos in place — collage now closely matches the reference's actual content (EPFO/ESIC text legible in Photo 2, handover gesture clearly visible in Photo 3), zero console errors at all three, no horizontal overflow on mobile (re-confirmed via script). **Pending final client sign-off on live site.**

CORRECTION (this thread, caught via direct user feedback with a live screenshot): the photo collage container used the full reference image's aspect ratio (1536:900) instead of the photo-collage column's own bounding-box aspect ratio (measured directly from the same reference: 869x748 ≈ 1.162:1). This squashed every element — Photo 2 rendered as an oval instead of a true circle, the badge was undersized and sat low/disconnected from the photo junction instead of nesting at it, and Photo 3 didn't extend far enough left, leaving a visible gap instead of the reference's interlocking composition. Recomputed all 4 positions (Photo 1/2/3 + badge) from the same original pixel measurements against the corrected column aspect ratio, and switched from percentage Tailwind classes to inline `style` left/top/width/height for precision. Re-verified at 1440px (Photo 2 is now a true circle, badge correctly sized and positioned at the junction, Photo 3 reaches under the badge as in the reference), 768px, and 390px — zero console errors at all three, no horizontal overflow on mobile.

CORRECTION (this thread, round 2 — caught via direct user feedback that the fix "still doesn't look like the reference"): created a scale-matched side-by-side comparison (reference crop resized to exactly match the live collage's rendered pixel dimensions, placed adjacent) rather than comparing two differently-sized crops by eye, which made the remaining errors obvious: Photo 1 was far too short (read as 42.9% tall in round 1, actually 67.6% of the column height), the badge was noticeably undersized (28.8%x33.4% vs the correct 42.2%x46.3%) and positioned too low/disconnected from the photo junction, and Photo 3 had a visible gap above it instead of tucking almost directly under the badge. Re-measured every element from scratch with a fine 20px grid overlay directly on the original reference image (the round-1 fix had used a coarser 25-50px grid, which was precise enough for Photo 2's circle but not for the others). Column bounding box recomputed as the true union of all 4 elements' extents: 830x735px, aspect 1.129:1 (different again from round 1's 869:748 estimate). Re-verified with a fresh scale-matched side-by-side: badge size/overlap, Photo 1's height, and Photo 3's tuck-under positioning now all closely match the reference. Re-verified at 1440px, 768px, 390px — zero console errors at all three, no horizontal overflow.

CORRECTION (this thread, round 3 — caught via direct user feedback that round 2's fix was "still totally messed up"): re-checked the live page directly and found the badge had been overcorrected in round 2 — at 42.2%x46.3% it now visibly engulfed most of Photo 1 and Photo 2, with excessive empty padding inside the circle relative to its actual content. Manual grid-reading proved unreliable specifically for this element across all 3 rounds (the orange ring is only ~2px thick and easy to misjudge against a busy, low-contrast office-photo background). Abandoned eye-measurement for the badge and instead detected it programmatically: isolated its solid white fill via colour-threshold + connected-component labelling (high contrast against the surrounding photos, unlike the thin ring against the page background), confirmed it forms a near-perfect circle (199x196px raw pixels), then added the ~2px ring stroke measured via direct pixel sampling along a line through the circle's centre. Final corrected size: 24.0%x27.2% of the column — these percentages are deliberately unequal (the column itself isn't square) but both resolve to ~199px in rendered pixels, preserving a true circle. Photo 1/2/3 positions re-verified against the same reference and confirmed unchanged from round 2 (Photo 1's measurement, in particular, was re-checked at high zoom and found to already be correct — the badge was the only real defect). Re-verified with a fresh scale-matched side-by-side comparison: badge size, position, and overlap with all 3 photos now closely match the reference. Verified at 1440px, 768px, 390px — zero console errors at all three, no horizontal overflow.

ROUND 4 (this thread): two further changes per direct user instruction.

(1) REMOVED the 20+/10+/All India stat-boxes row entirely from the Hero. This row does appear in the reference image too (lower in the layout), so this is a deliberate content/scope decision rather than a visual-accuracy correction — the client felt it was taking up unwanted space. `hero.stats` left in the data file, unused, in case a similar row is wanted elsewhere later; removed the now-orphaned Counter import and the whole stat-boxes block from the component.

(2) Re-measured the ENTIRE photo collage from scratch, having recognised that rounds 1-3 each fixed one element while leaving others wrong because manual grid-reading on separately-cropped sub-images produces inconsistent boundaries when elements actually touch/overlap with no background gap — confirmed via content-vs-background masking that Photo 1/2/3 and the badge form one single connected region, not 4 independently-measurable shapes. This time read every boundary from ONE continuous grid overlay spanning the full collage (not stitched crops), then cross-validated both circles (Photo 2, the badge) by checking their measured width and height come out equal — a real circle must satisfy this, and using it as a check caught remaining errors. Final verified measurements (1536x1024 reference): Photo 1 665-940x135-638 (275x503), Photo 2 1062-1450x140-528 (388x388, true circle), badge centred (1070,531) radius 99 (198x198, true circle, matches the round-3 programmatic detection almost exactly), Photo 3 935-1450x565-835 (515x270 — much wider/shorter than rounds 1-2 assumed; its left edge tucks under the badge and isn't directly visible, corroborated by dark-hair content peeking out from under the badge's curve). Column bounding box: 785x700px, aspect 1.121:1.

Re-verified with a fresh scale-matched side-by-side comparison: badge size/position/overlap with all 3 photos, Photo 1's proportions, and Photo 3's wider rectangle now all closely match the reference — the closest match across all 4 rounds. Verified at 1440px, 768px, 390px — zero console errors at all three, no horizontal overflow, stat-boxes row confirmed fully removed at all breakpoints.

| # | Section | Status |
|---|---------|--------|
| 1 | Hero | Round 4 in progress — see full notes above, pending sign-off |
| 2 | Overview | Pending review |
| 3 | Applicability | Pending review |
| 4 | Documents | Pending review |
| 5 | Process | Pending review |
| 6 | Penalties | Pending review |
| 7 | FAQs | Pending review |
| 8 | Related Services | Pending review |
| 9 | Final CTA | Pending review |

---

Tell me where to start — section number, what kind of change, and any
screenshots/frames/description you have. I'll make the change, rebuild,
push, and you review before moving to the next section.
